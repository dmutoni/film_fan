import 'package:flutter/material.dart';

class SingleMovie extends StatelessWidget {
  const SingleMovie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('This a single movie');
  }
}
